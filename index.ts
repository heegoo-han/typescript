// when creating a github repository, typescript
// I selected .gitignore type as "node"

function sum(a: number, b: number): number {
    return a + b;
}
console.log(sum(10,20));

// open terminal and then
// ~/src/typescript > npm init -y
// it will make package.json